const { Given,Then,When } = require('cucumber');

Given(/^I am on the phptravels page$/, function() {
});

When(/^I login with username and password "user@phptravels.com" "demouser" into the text box$/, function() {
});

Then(/^I should see the message "Hi, Johny Smith" on my account page$/, function() {
    
  });